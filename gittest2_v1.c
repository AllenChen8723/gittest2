#include <stdio.h>h
#include <stdlib.h>



int main(int argc, char const *argv[])
{
    int a =256;
    printf("now is in the git_test_version2");

    printf("now is in the git_test_version2");
    
 
    return 0;
}
